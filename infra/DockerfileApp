FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] pymilvus xarray zarr gcsfs fsspec tqdm geojson-pydantic area geojson shapely pyproj

COPY api/app.py .

EXPOSE 8000

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]