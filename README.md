# Earth Embeddings Sandbox

A little repo to explore and play with earth embeddings

## Description

## Plan

[] extract embeddings for a custom AOI and get them into GCS public bucket
[] mean-pool them to different zoom levels (ndpyramid)
[] render and re-render them via a front end -> NICE
[] put the mean-pooling into a vector db
[] expose a spatial sampling tool and surface nearest neighbours vai fastapi

## Setup
